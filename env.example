# Saved automation webhook (My Automations → Triggers → Doculytix V1)
USE_HACHI_INBOX=0
HACHI_WEBHOOK_URL=
HACHI_TRIGGER_SECRET=
HACHI_INBOX_URL=

# Only used when USE_HACHI_INBOX=1 (global inbox — URL must end with /webhooks/inbox)
# HACHI_INBOX_URL=
# HACHI_INBOX_PROMPT="Process this invoice: extract vendor name, invoice number, line items, subtotal, tax, and total."

DEFAULT_DOCUMENT_URL=
PORT=8080
